const Discord = require("discord.js");

module.exports = (client) => {

    client.log("DISCONNECT", "Bot has disconnected, reconnecting now");
    console.log('Bot just disconnected! I wll reconnect now...');

}
